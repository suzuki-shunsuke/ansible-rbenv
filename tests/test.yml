---
- include: setup.yml
- hosts: default
  roles:
  - suzuki-shunsuke.ghq-module
  - role: ansible-rbenv
    ghq_executable: "{{'{}/bin/ghq'.format(ansible_env.get('GOPATH', '{}/.go'.format(ansible_env.HOME)))}}"
